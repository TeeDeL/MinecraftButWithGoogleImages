package com.teedel.MinecraftButWithGoogleImages.tester;

import com.teedel.MinecraftButWithGoogleImages.model.FormatFix;
import com.teedel.MinecraftButWithGoogleImages.model.ImageParse;
import com.teedel.MinecraftButWithGoogleImages.model.ImageResizeTool;
import com.teedel.MinecraftButWithGoogleImages.model.NameMapper;

public class QuickTestFornameMapper {
    /*
    public static void main(String[] args) {
        NameMapper doa = new NameMapper();

        for(String as : doa.getNameList())
        {
            new ImageParse(as);
            String val = new FormatFix().addUnderscore(as);
            try {
                new ImageResizeTool(256, 256, val);
            } catch (Exception e){
e.printStackTrace();
            }
        }

 private static String nameFormatter(String originalText)
    {
        String formattedText = originalText.replace(".png", "");
        formattedText = formattedText.replace("_", " ");
        return formattedText;
    }

         */



}
